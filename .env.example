# Database
# For local development (SQLite):
# DATABASE_URL="file:./dev.db"
# For production (Vercel Postgres - auto-generated in Vercel):
DATABASE_URL="your-postgres-connection-string"
DIRECT_URL="your-postgres-direct-connection-string"

# NextAuth
# Generate with: openssl rand -base64 32
AUTH_SECRET="your-generated-secret"
AUTH_URL="http://localhost:3000"

# Google OAuth (Get from Google Cloud Console)
# https://console.cloud.google.com/apis/credentials
AUTH_GOOGLE_ID="your-google-client-id"
AUTH_GOOGLE_SECRET="your-google-client-secret"

# GitHub API (optional, for playground data)
GITHUB_TOKEN=""

# Development Only - Bypass auth (REMOVE IN PRODUCTION!)
BYPASS_AUTH="true"

# Vercel Blob Storage (auto-generated on Vercel when you enable Blob)
# BLOB_READ_WRITE_TOKEN="vercel_blob_token"
